#include<iostream>
#include<conio.h>
#include<stdio.h>

using namespace std;

int main(){

// getting string from the user
// string two types can be use char and string class (later will talk)
char name[0];
cout<< "enter your name: ";
gets(name);
//cin>>name;

cout<< "name: - "<< name;

getch();
}
